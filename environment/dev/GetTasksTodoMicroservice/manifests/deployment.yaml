apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-get-task-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: todo-get-task-api
  template:
    metadata:
      labels:
        app: todo-get-task-api
    spec:
      containers:
      - name: todo-get-task-api
        image: akscon1.azurecr.io/todo-get-task-api:v1
        ports:
        - containerPort: 8000
        env: 
        - name: CONNECTION_STRING
          valueFrom:
            secretKeyRef:
              name: db-secret
              key: CONNECTION_STRING
          

