apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todo-delete-tasks-api-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: todo-delete-tasks-api-kds14.shop
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: todo-delete-tasks-api
                port:
                  number: 80
